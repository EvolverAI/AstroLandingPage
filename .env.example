# Environment variables for different deployment scenarios

# For local development (default)
# PUBLIC_BASE_URL=/

# For subdirectory deployment (e.g., github pages with repo name)
# PUBLIC_BASE_URL=/your-repo-name/

# For custom domain (production)
# PUBLIC_BASE_URL=/

# Asset path prefix (if you need to serve assets from a different location)
# PUBLIC_ASSETS_PREFIX=/

# Image optimization service (optional)
# PUBLIC_IMAGE_SERVICE=local
